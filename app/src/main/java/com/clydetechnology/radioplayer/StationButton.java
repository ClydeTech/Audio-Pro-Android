package com.clydetechnology.radioplayer;
import android.content.Context;
        import android.util.AttributeSet;
        import android.widget.Button;
        import androidx.appcompat.widget.AppCompatButton;

public class StationButton extends AppCompatButton {
    public StationButton(Context context) {
        super(context);
    }

    public StationButton(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public StationButton(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}